---
id: A-0001
kind: attack
title: Unknown FROM-position table functions
status: active
severity: critical
tags: [manifest, duckdb, table-functions, escape]
origin:
  date: 2026-07-24
  repo: cortex
  incident: "parquet_metadata, parquet_schema, read_json_objects_auto, iceberg_metadata bypass"
occurrences: 4
enforced_by: tests/test_execution/hostile_sql_corpus.json
related: [W-0004, R-0005]
supersedes: []
---

## Attack

Probe FROM-clause table functions not on the allowlist: `parquet_metadata`, `parquet_schema`,
`read_json_objects_auto`, `iceberg_metadata`, and variants. A denylist wearing an allowlist's clothes.

## What it found

Reads paths outside manifest grant via table functions in FROM position.

## Corpus case

Add to `hostile_sql_corpus.json` — cases may be added, never removed.

## Reuse

Any manifest/table-function hardening pass. Run before claiming FROM-position closed.
