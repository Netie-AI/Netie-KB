---
id: R-0016
kind: rule
title: Distilled skills live in Netie-KB and are served from one registry
status: active
severity: high
tags: [skills, distill, mcp, registry, sync]
origin:
  date: 2026-08-25
  repo: netie-kb
  incident: "skills existed as scattered intents (Grok Bot menu, empty Internal/Skills, empty KB skills/) with no place any lane could query"
occurrences: 1
enforced_by: null
related: [R-0009, R-0013, R-0015]
supersedes: []
---

## Rule

Every distilled skill, rule, workflow, and finding lands in D:\Netie-KB via
`kb.py new` and syncs through the GitHub remote (Netie-AI/Netie-KB). Before
authoring or re-deriving a skill, search the registry first: locally
`python D:\Netie-KB\scripts\kb.py search "<keywords>"`, remotely the KB skill
server (`scripts/skill_server.py`, MCP + REST, default 127.0.0.1:8030).
Cloud agents and containers either clone the KB or call the hosted server;
no lane keeps a private skills folder. Per-repo rule packs (CLAUDE.md,
netie-kb.mdc) are rendered outputs of this corpus, never hand-edited.

## Why

1. 2026-08-25: customer-chat / pptx / proposal / laptop-use skills existed only as
   intents scattered across Grok Bot's menu and empty folders (Internal/Skills = 0,
   KB skills/ = 0); every lane re-derived instead of pulling from one shelf.
2. GROK_SYNC.md already routes Grok skill export to skill_distill -> KB; a served
   registry closes the loop for every lane, including cloud containers that never
   see this laptop's disks.

## Check

`kb.py validate` passes; skill server `/healthz` reports the same artifact counts
as `iter_artifacts()`; a skill filed with `kb.py new skill` appears in server
search results with no extra registration step.
