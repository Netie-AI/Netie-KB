---
id: F-0038
kind: finding
title: Crew roles/agents unread vs none; analog refuses instance-data and .local
status: active
severity: medium
tags: [crew, r-0011, analog, ssrf]
origin:
  date: 2026-09-03
  repo: cortex
  incident: "engine Crew UI roles chip bar stayed empty on failed GET; analog_clone would fetch instance-data and *.local"
occurrences: 1
enforced_by: tests/test_crew/test_server.py tests/test_crew/test_research.py tests/test_crew/test_analog.py
related: [R-0011, F-0036]
supersedes: []
---

## Expected vs actual

Expected inspector/composer panes to name unread vs idle none, and analog_clone to refuse metadata hostnames that are not literal IPs. Actual: GET /crew/roles catch returned [] and renderRoles returned without painting; agents/todos/confirms first-painted as none/pending; instance-data and printer.local passed refuse_nonpublic_url because they are names, not IPs.

## Reproduction

PYTHONPATH=E:\Cortex python -c "from CortexOS.crew.research import refuse_nonpublic_url; print(refuse_nonpublic_url('http://instance-data/')); print(refuse_nonpublic_url('http://printer.local/'))" -- was None, now a reason. GET / on TestClient index includes "roles unread" and "agents unread".

## Root cause class

Empty default vs failed fetch (R-0011). Hostname allow-list missed mDNS and AWS instance-data after IP-class (is_global) was already tight.

## Invariant gap

R-0011: an empty chip bar and a healthy-but-quiet chip bar look identical. A private analog fetch that looks public is a lie.

## Notes

IPv6 ::1, mapped RFC1918, metadata.google.internal, CGNAT 100.64/10 already refused via is_global / .internal -- tests pinned, no rewrite. queue.py wakes.py stall.py a2a.py untouched. Live :8020 is still the Cortex-crew fork. Cite: docs/subagents_findings/2026-09-03_crew-roles-mdns-metadata.md
