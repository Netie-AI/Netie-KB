---
id: F-0001
kind: finding
title: Drillthrough projected provenance onto warehouse tables without _src columns
status: unverified
severity: critical
tags: [drillthrough, t7, provenance, warehouse]
origin:
  date: 2026-07-31
  repo: cortex
  incident: "POST /v1/contract/drillthrough returned 500 on live Top-5 token"
occurrences: 1
enforced_by: tests/test_execution/test_drillthrough.py
related: [R-0001, R-0005]
supersedes: []
---

## Expected vs actual

Expected contributing rows for a governed Top-5 answer. Actual: Cortex 500;
EXPLAIN Binder Error  -  `transactions` has no `_src_ref_id`. Nested
`ROUND(COALESCE(SUM(...)))` also left SUM after GROUP BY strip.

## Reproduction

1. Live ask `Top 5 selling SKUs by revenue` -> mint drillthrough_token
2. `POST /v1/contract/drillthrough` with token -> 500

## Root cause class

Provenance projection assumed lakehouse lineage columns on every FROM table;
aggregate strip only matched top-level AggFunc, not wrappers.

## Invariant gap

R-0005 (control refusing legitimate work) + R-0001 (customer artifact  - 
Source panel dead).

## Notes

Fixed: `include_provenance` fallback on binder miss; recursive `_unwrap_aggregate`.
